class Product {
  final String id, brandName, title, price, url;

  const Product({
    required this.id,
    required this.brandName,
    required this.title,
    required this.price,
    required this.url,
  });
}
